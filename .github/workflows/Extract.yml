name: Extract ZIP to Main Root

on:
  workflow_dispatch:

jobs:
  unzip:
    name: Extract Uploaded ZIP to Root
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Unzip contents to root (flatten folder)
        run: |
          mkdir temp
          unzip -q Bwm-trekker-6f70a5dea5e66e8712f51cb4c8dbd1a4d0a76cd9(1).zip -d temp
          mv -n temp/Bwm-trekker-6f70a5dea5e66e8712f51cb4c8dbd1a4d0a76cd9(1)/* .
          rm -rf temp
          rm Bwm-trekker-6f70a5dea5e66e8712f51cb4c8dbd1a4d0a76cd9(1).zip

      - name: Commit unzipped files to main
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add .
          git commit -m "Extract and flatten ZIP contents to repo root" || echo "No changes to commit"

      - name: Push changes
        run: |
          git push origin HEAD:main
